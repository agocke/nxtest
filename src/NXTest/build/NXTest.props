<Project>
  <!-- NXTest generates a small dispatcher so direct benchmark runs can bypass MTP. -->
  <PropertyGroup>
    <GenerateTestingPlatformEntryPoint>false</GenerateTestingPlatformEntryPoint>
  </PropertyGroup>
</Project>
